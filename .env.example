# 后端 API 基础 URL
# 开发环境
VITE_API_BASE_URL=http://localhost:8081/api

# 生产环境
# VITE_API_BASE_URL=https://api.example.com/api

# ---- 数据源开关（分类等数据的传输方式）----

# 是否启用前端 Mock（前端独立开发时开启，由 src/api/mock.ts 返回固定数据）
# VITE_USE_MOCK=true

# 是否启用真实后端接口（后端就绪后开启；此时建议同时关闭 Mock）
# VITE_USE_BACKEND=true
